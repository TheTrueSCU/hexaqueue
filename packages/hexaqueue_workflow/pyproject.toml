[project]
name = "hexaqueue-workflow"
version = "0.0.0"
description = "Hexaqueue Workflow - Temporal Activity Worker & Argo Workflow Task Driver"
readme = "README.md"
license = "Apache-2.0"
authors = [
    { name = "Richard West", email = "dopplereffect.us@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "hexaqueue-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
hexaqueue-core = { workspace = true }
